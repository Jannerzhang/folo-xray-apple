# Security policy

Report vulnerabilities through the repository's private security-reporting channel when enabled. Until then, contact the repository administrator through the organization's private security channel.

Do not include production credentials, node configuration, signing material, or user data in reports. Public advisories should identify affected source tags and artifact hashes without disclosing active secrets.
